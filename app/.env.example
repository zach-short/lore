# Copy to app/.env. From your Supabase project: Settings → API Keys.
# Use the publishable (or legacy anon) key — never the secret/service key here.
# Missing values run the app open with auth disabled (local dev fallback).
EXPO_PUBLIC_SUPABASE_URL=
EXPO_PUBLIC_SUPABASE_KEY=
